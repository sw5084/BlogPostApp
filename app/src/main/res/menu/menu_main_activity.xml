<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/post_mode"
        android:title="Change Post Mode"
        android:icon="@drawable/list_mode"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/userprofile"
        android:title="Profile"
        android:icon="@drawable/my_profile"
        app:showAsAction="ifRoom"/>
    <item android:id="@+id/refreshlist"
        android:title="Refresh Page" />
    <item android:id="@+id/signout"
        android:title="Sign out" />
</menu>